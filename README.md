# Notas:
Esta es una practica de Node Js

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     argumento para especificar base de la tabla a crear
                                                             [number] [required]
  -l, --listar   argumento para mostrar la tabla o no [boolean] [default: false]
  -h, --hasta    Establece el limite al que llegara la tabla
                                                          [number] [default: 10]
```
